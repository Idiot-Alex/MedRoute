# 前端架构与 OpenLayers 技术选型

## 1. 结论

新版前端采用：

| 范围 | 技术 | 职责 |
| --- | --- | --- |
| 工程 | Vue 3、TypeScript、Vite、pnpm workspace | 两个独立应用和共享包 |
| UI | Tailwind CSS | 布局、表单、工具栏和响应式界面 |
| 地图 | OpenLayers | 静态楼层底图、矢量路网、编辑交互和移动手势 |
| 图计算 | Spring Boot 后端 | 正式 Dijkstra 路线计算和发布版本约束 |

不采用前端路线计算库。移动端只提交起终点和路线模式，展示后端返回的楼层路线段
与跨层动作。

## 2. 选择 OpenLayers 的原因

当前地图不是地理坐标地图，而是：

```text
独立楼层位图
  + 像素坐标节点
  + 由节点 ID 连接的路径
  + 绑定节点的 POI
  + 电梯、楼梯停靠点和明确层间连接
```

OpenLayers 支持像素投影和静态图片，也原生提供 Feature 命中、选择、平移、
修改、绘制、吸附、图层和手机缩放手势。一套库可以同时服务维护端和导航端，
减少两套渲染器造成的坐标、样式和命中行为差异。

备选方案：

- Konva 适合高度定制的 Canvas 编辑器，但地图缩放、吸附和图数据关系需要更多
  自定义代码。如果 OpenLayers 编辑体验验证失败，再单独评估。
- Leaflet `CRS.Simple` 适合轻量只读查看，但编辑依赖额外插件，同时会引入第二套
  坐标和地图 API，不作为当前主方案。
- Fabric.js、Vue Flow 和 PixiJS 分别偏通用设计器、流程图和底层高性能渲染，
  与不可变底图上的导航图维护不匹配。
- MapLibre、IMDF 编辑器面向地理配准或室内墙体、房间矢量化，超出当前产品范围。

## 3. 工作区结构

```text
frontend/
├── apps/
│   ├── map-admin-web/
│   └── navigation-web/
├── packages/
│   ├── api-client/
│   └── map-core/
└── public/
```

`map-admin-web` 和 `navigation-web` 独立构建、独立发布。共享包只提供接口、领域
规则、坐标和地图基础能力，不共享具体页面布局。

## 4. 数据和坐标边界

后端 DTO 是唯一可持久化数据源。OpenLayers Feature 是视图模型，不能被序列化为
另一套草稿格式。

数据库使用楼层图片左上角为原点：

```text
dataX = imageX
dataY = imageY
```

OpenLayers 像素投影使用 Y 轴向上：

```text
mapX = dataX
mapY = imageHeight - dataY
```

正反转换统一位于 `packages/map-core/src/coordinates.ts`。路径只持久化
`fromNodeId` 和 `toNodeId`，显示几何由节点坐标生成。拖动节点时同步移动绑定 POI，
并重新生成相连路径。

## 5. Tailwind 边界

Tailwind 负责页面 UI，不负责 OpenLayers Canvas 内 Feature 的动态样式。

- 工具栏、表单、侧边栏、模式切换和移动布局使用 Tailwind。
- 节点、路径、POI、停靠点和路线使用 OpenLayers Style API。
- 保留 `ol/ol.css`，对地图容器内图片和 Canvas 明确设置 `max-width: none`，
  避免 Tailwind Preflight 改变地图资源尺寸。
- Tailwind 类名保持静态可分析，不拼接运行时类名。

采用 Tailwind 4 前，生产设备应满足 Chrome 111、Safari 16.4 或 Firefox 128
及以上。若宿主 App 使用更旧 WebView，需要降低版本或保留兼容 CSS。

## 6. 当前实现

已经完成：

1. pnpm 工作区和两个 Vue 应用。
2. 与 Java DTO 对齐的 TypeScript 类型和统一 API 客户端。
3. 真实三层公开测试底图的像素投影。
4. 节点、路径、POI、跨层停靠点分层显示。
5. 维护端节点新增、拖动、两节点吸附连边、POI 绑定和草稿保存。
6. 导航端起终点、常规/无障碍模式、后端路线调用、楼层路线段和跨层步骤。
7. 无后端 `demo=1` 演示模式。
8. 电梯和楼梯设施创建、停靠点标注、跨层连接和属性编辑。
9. 跨层关系本地检查，并区分“阻止草稿保存”和“发布前修复”问题。
10. 发布版本、历史版本和草稿切换，以及草稿创建和放弃。
11. 关键路线配置、发布前自动回归和失败发布门禁。
12. 后端完整校验结果展示和地图对象定位。
13. 校验通过后发布、发布原因记录和历史版本回滚。
14. 楼层底图替换前后预览、非等比缩放警告和受影响标注统计。
15. 移动端完整 POI 搜索、楼层筛选、别名和拼音维护关键词检索。
16. 稳定 POI 编码深链、旧参数兼容和失效地点回退。
17. 固定路线逐条说明、前后切换和当前步骤楼层联动。
18. 当前发布版本运营封闭、恢复、刷新和地图对象定位。
19. 当前发布 POI 固定点二维码地址、复制和 PNG 下载。
20. 右侧固定页签栏、独立内容滚动和运营视图按需加载。
21. 节点、路径和 POI 全部后端字段、同层端点/绑定节点重绑及搜索词维护。
22. 节点硬引用、POI 关键路线引用检查、依赖跳转和安全删除。
23. 单击未移动节点不再错误标记草稿修改，无效引用重绑后控件自动回退。

新版维护端已覆盖静态维护后台当前功能，静态页面仅保留为兼容验收基线。

`demo=1` 中的草稿、发布和回滚只存在于当前维护页面会话，不写数据库，也不会
改变另一个标签页中的导航演示数据。连接真实后端时，发布和回滚才会切换导航端
读取的当前发布版本。

跨层关系采用显式建模：设施只包含它实际停靠的楼层，只有已配置的停靠点对才可
导航。电梯可以仅停 1F 和 3F，并直接维护 1F ↔ 3F 连接；系统不会根据楼层顺序
自动补出 2F。

## 7. 验证结果

| 项目 | 结果 |
| --- | --- |
| 像素坐标正反转换单元测试 | 通过 |
| 全工作区 TypeScript 类型检查 | 通过 |
| 两个应用生产构建 | 通过 |
| 维护端 `1200 x 720`、`1440 x 900` | 无横向溢出，三栏和地图可用 |
| 导航端 `360 x 800`、`390 x 844`、`430 x 932` | 无横向溢出，地图和表单可用 |
| 节点新增、拖动和路径创建 | 通过浏览器验证 |
| 设施创建、两层停靠点标注和跨层连接 | 通过浏览器验证 |
| 跨层关系校验单元测试 | 4 个场景通过 |
| 发布门禁和校验结果映射单元测试 | 4 个场景通过 |
| 底图尺寸风险和本层坐标缩放单元测试 | 4 个场景通过 |
| API multipart、ETag 和空响应契约测试 | 3 个场景通过 |
| 全工作区单元测试 | 7 个文件、34 个场景通过 |
| 草稿创建、完整校验、发布和历史版本回滚 | 通过浏览器验证 |
| 关键路线修改、校验失效和失败发布门禁 | 通过浏览器验证 |
| 底图弹窗、发布版本只读和未保存修改保护 | 通过浏览器验证 |
| 同楼层及跨楼层路线、楼层切换 | 通过浏览器验证 |
| 名称/别名搜索、稳定深链失效回退 | 通过浏览器验证 |
| 固定路线步骤推进和跨层地图联动 | 通过浏览器验证 |
| 运营封闭创建、地图定位和恢复 | 通过浏览器验证 |
| 二维码正式/本机地址、旧参数清理和后端失联回退 | 通过浏览器验证 |
| 后端运营 API 和可解码二维码集成测试 | 4 个场景通过 |
| 完整属性、有效/无效引用重绑和依赖跳转 | 通过浏览器验证 |
| 独立元素删除、节点硬引用和 POI 关键路线删除保护 | 通过浏览器验证 |
| 单击节点不污染草稿修改状态 | 通过浏览器验证 |
| 浏览器警告和错误 | 修复数字上限解析问题后无新增 |

当前导航端生产 JavaScript 约 `123.3 KB gzip`；维护端主包约 `151.9 KB gzip`，
运营视图按需加载约 `3.3 KB gzip`，基础属性检查器按需加载约 `4.0 KB gzip`。
后续功能增加时继续按低频视图拆分异步模块，并持续观察移动端首次加载资源。

## 8. 下一阶段

1. 用真实文件连接后端复核底图上传、坐标缩放和草稿放弃恢复。
2. 连接真实后端复核运营封闭绕行、二维码 PNG 和扫码深链。
3. 用实际医院电脑、手机和宿主 App WebView 执行现场验收。
4. 功能稳定后通过可替换认证适配器接入宿主项目，不在当前前端绑定登录实现。
